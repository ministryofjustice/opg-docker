input {
    redis {
        host => 'localhost'
        data_type => list
        key => 'logstash'
        type => 'logstash:beaver'
    }
}

input {
    tcp {
        port => 514
        type => syslog
    }
    udp {
        port => 514
        type => syslog
    }
}

output {
    stdout { }
    elasticsearch {
        embedded => true
        flush_size => 1
    }
}

